spring:
  #数据源配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vote?serverTimezone=UTC
    username: root
    password: MZHmzh2016
  #thymeleaf配置
  thymeleaf:
    cache: false
    mode: HTML5
    encoding: UTF-8
    servlet:
      content-type: text/html

mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml
  type-aliases-package: com.lepisori.voting.model

server:
  #服务端口
  port: 8080






